import loops from plugin loading
config loader / commands w config / better server startup
create users command
create users UI
basic auth "login"
super basic view of news items UI
subscription management UI
caching
basic styling
basic curation / tagging

=defer=
server reload on file change
plugin ordering and overriding
split out users app
auth a la django auth backends
openid
running tasks w/o cron
pubsubhub
twitter
shell
sphinx docs
basic filtering
cleanup tasks
paver / installation
split framework